# Lint conservador: errores reales (pyflakes + sintaxis), sin ruido de estilo,
# para no romper el codigo existente el dia 1. Endurecer mas adelante.
line-length = 120

[lint]
select = ["E9", "F"]

[lint.per-file-ignores]
"tests/*" = ["F401"]
